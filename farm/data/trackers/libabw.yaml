pkg-name: libabw
type: script
script-content: |
  page=$(curl -fsSL "https://dev-www.libreoffice.org/src/libabw/")
  ver=$(printf '%s\n' "$page" | grep -oE "libabw-[0-9.]+\.tar\.xz" | sed "s/^libabw-//; s/\.tar\.xz$//" | sort -V | tail -n1)
  test -n "$ver" || exit 1
  echo "$ver"
  echo "https://dev-www.libreoffice.org/src/libabw/libabw-$ver.tar.xz"
