pkg-name: libqxp
# release 托管在 dev-www.libreoffice.org（download.gnome.org 没有）→ script 探测 LO 镜像
tracker-template: script
script-content: |
  page=$(curl -fsSL "https://dev-www.libreoffice.org/src/libqxp/")
  ver=$(printf '%s\n' "$page" | grep -oE "libqxp-[0-9.]+\.tar\.xz" | sed "s/^libqxp-//; s/\.tar\.xz$//" | sort -V | tail -n1)
  test -n "$ver" || exit 1
  echo "$ver"
  echo "https://dev-www.libreoffice.org/src/libqxp/libqxp-$ver.tar.xz"
