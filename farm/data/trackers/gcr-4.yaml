# gcr-4（GTK4 线）——同源 gcr 的 4.x；gcr 3.x(gtk3) 另包 gcr。
# 同源惯例「.90 以上是过渡版号」：4.x 内若有 4.90+ 过渡（pre-5.0）也不跟 → cap 4.89.99。
pkg-name: gcr-4
version-source: sources[0]
sources:
- tracker-template: gnome
  source-name: gcr
  stable-minor: all
  max-version: 4.89.99
  template: https://download.gnome.org/sources/gcr/{path_version}/gcr-{version}.tar.xz
