pkg-name: lager
version-source: sources[0]
sources:
- tracker-template: github
  repo: arximboldi/lager
  mode: tags
  tag-prefix: v
  template: https://github.com/{repo}/archive/{tag}/{name}-{version}.tar.gz
