pkg-name: docker
version-source: sources[0]
sources:
- tracker-template: github
  repo: docker/cli
  mode: tags
  tag-prefix: v
  template: https://github.com/{repo}/archive/refs/tags/{tag}.tar.gz
- tracker-template: same-version
  repo: moby/moby
  mode: tags
  tag-prefix: docker-v
  template: https://github.com/{repo}/archive/refs/tags/{tag}.tar.gz
  same-version-of: docker
- tracker-template: github
  repo: krallin/tini
  mode: tags
  tag-prefix: v
  template: https://github.com/{repo}/archive/refs/tags/{tag}.tar.gz
