pkg-name: libodfgen
type: script
script-content: |
  page=$(curl -fsSL "https://dev-www.libreoffice.org/src/")
  ver=$(printf '%s\n' "$page" | grep -oE "libodfgen-[0-9.]+\.tar\." | sed "s/^libodfgen-//; s/\.tar\..*$//" | sort -V | tail -n1)
  test -n "$ver" || exit 1
  echo "$ver"
  echo "https://dev-www.libreoffice.org/src/libodfgen-$ver.tar.xz"
