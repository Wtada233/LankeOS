#!/bin/bash

lankebuild_prepare() {
    # Prepare environment
    :
}

lankebuild_build() {
    sed -i '/$(MAKE) -C doc/d' Makefile
    make -j$(nproc)
    make PREFIX={PREFIX} DESTDIR={STAGING_ROOT} install
    rm -f {STAGING_ROOT}{LIBDIR}/libgif.a
}


lankebuild_package() {
    # Installation handled by builder
    :
}
