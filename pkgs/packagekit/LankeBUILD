lankebuild_prepare() {
    :
}

lankebuild_build() {
    meson setup build --prefix={PREFIX} --sysconfdir={SYSCONFDIR} -Dgtk_doc=false .
    ninja -C build -j$(nproc)
    DESTDIR={STAGING_ROOT} ninja -C build install
}

lankebuild_package() {
    # Installation handled by builder
    :
}
