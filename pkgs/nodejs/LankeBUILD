lankebuild_prepare() {
    ./configure --prefix={PREFIX}  \
            --ninja                \
            --enable-lto           \
            --shared-nghttp2       \
            --shared-openssl       \
            --shared-zlib          \
            --with-intl=system-icu \
            --shared-brotli        \
            --shared-ffi           \
            --shared-nghttp2       \
            --shared-openssl       \
            --shared-zlib          \
            --shared-zstd
}

lankebuild_build() {
    make -j$(nproc)
}

lankebuild_package() {
    make DESTDIR={STAGING_ROOT} install
}
