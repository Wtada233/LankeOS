lankebuild_prepare() {
    mkdir -p build
}

lankebuild_build() {
    echo "Configuring gst-plugins-bad-{ORIG_PKG_VER}"
    cd build
    meson setup .. --prefix={PREFIX} --sysconfdir={SYSCONFDIR}        \
                   --buildtype=release  \
                   -Ddoc=disabled       \
                   -Dintrospection=enabled \
                   -Dnls=disabled       \
                   -Dtests=disabled     \
                   -Dexamples=disabled
    echo "Building gst-plugins-bad-{ORIG_PKG_VER}"
    ninja -j$(nproc)
    echo "Build complete."
}

lankebuild_package() {
    echo "Packaging gst-plugins-bad-{ORIG_PKG_VER}"
    cd build
    DESTDIR="{STAGING_ROOT}" ninja install
    echo "Package staging complete."
}
