#!/bin/bash

lankebuild_prepare() {
    # Prepare environment
    :
}

lankebuild_build() {
    sed -i '/long long t1;/,+1s/()/(...)/' configure
    ./configure --prefix={PREFIX} --sysconfdir={SYSCONFDIR} --enable-cxx --disable-static --disable-doc
    make -j$(nproc)
    make DESTDIR={STAGING_ROOT} install
}


lankebuild_package() {
    # Installation handled by builder
    :
}
