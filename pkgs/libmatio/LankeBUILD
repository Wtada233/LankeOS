lankebuild_prepare() {
    # github 归档缺 configure，重新引导
    autoreconf -fiv
}

lankebuild_build() {
    ./configure --prefix={PREFIX} --enable-shared --disable-static --with-hdf5
    make -j$(nproc)
    make DESTDIR={STAGING_ROOT} install
}

lankebuild_package() {
    :
}
