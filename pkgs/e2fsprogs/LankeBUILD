#!/bin/bash

lankebuild_prepare() {
    # Prepare environment
    :
}

lankebuild_build() {
    mkdir build && cd build
    ../configure --prefix={PREFIX} --sysconfdir={SYSCONFDIR} --enable-elf-shlibs --disable-static --disable-libblkid --disable-libuuid --disable-fsck
    make -j$(nproc)
    make DESTDIR={STAGING_ROOT} install
    find {STAGING_ROOT}{LIBDIR} -name "*.a" -delete
}


lankebuild_package() {
    # Installation handled by builder
    :
}
