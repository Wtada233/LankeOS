#!/bin/bash
lankebuild_prepare() {
    :
}
lankebuild_build() {
    ./configure --prefix={PREFIX} --sysconfdir={SYSCONFDIR} --disable-static
    make -j$(nproc)
    make DESTDIR={STAGING_ROOT} install
    rm -f {STAGING_ROOT}{LIBDIR}/libnewt.a
}
lankebuild_package() {
    :
}
