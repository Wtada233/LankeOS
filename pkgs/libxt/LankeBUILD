lankebuild_prepare() {
    ./autogen.sh
    ./configure --prefix=/usr --sysconfdir=/etc --disable-static --disable-docs
}
lankebuild_build() {
    cd "{SRC_DIR}"
    make -j$(nproc)
}
lankebuild_package() {
    cd "{SRC_DIR}"
    make DESTDIR="{STAGING_ROOT}" install
}
