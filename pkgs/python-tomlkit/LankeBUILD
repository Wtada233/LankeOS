lankebuild_prepare() {
    :
}

lankebuild_build() {
    # PEP 517 wheel 构建（poetry-core backend，需系统 python-poetry-core）
    python3 -m build --wheel --no-isolation
}

lankebuild_package() {
    python3 -m installer --destdir={STAGING_ROOT} dist/*.whl
    find {STAGING_ROOT}/ -name "__pycache__" -type d -exec rm -rf {} +
}
