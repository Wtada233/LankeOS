lankebuild_prepare() {
    mkdir -p build
}

lankebuild_build() {
    echo "Configuring gstreamer-{ORIG_PKG_VER}"
    cd build
    meson setup .. --prefix={PREFIX} --sysconfdir={SYSCONFDIR}        \
                   --buildtype=release  \
                   -Ddoc=disabled       \
                   -Dtests=disabled     \
                   -Dexamples=disabled  \
                   -Dintrospection=enabled \
                   -Dnls=disabled       \
                   -Dtools=disabled
    echo "Building gstreamer-{ORIG_PKG_VER}"
    ninja -j$(nproc)
    echo "Build complete."
}

lankebuild_package() {
    echo "Packaging gstreamer-{ORIG_PKG_VER}"
    cd build
    DESTDIR="{STAGING_ROOT}" ninja install
    echo "Package staging complete."
}
