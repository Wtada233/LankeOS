lankebuild_prepare() {
    # Prepare environment
    :
}

lankebuild_build() {
    meson setup build \
        --prefix={PREFIX} \
        -Dtests=disabled
    meson compile -C build
    DESTDIR={STAGING_ROOT} meson install -C build
}

lankebuild_package() {
    # Installation handled by builder
    :
}
