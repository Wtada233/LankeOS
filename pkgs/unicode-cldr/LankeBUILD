# unicode-cldr — Unicode CLDR（ibus 的 unicode-parser 构建期读 annotaions/main 生成名称字典）
# Arch 把它拆成 unicode-cldr + unicode-cldr-annotations 两个包；本仓库并一包（拆包的动机只是省依赖）。

lankebuild_prepare() {
    :
}

lankebuild_build() {
    # 纯数据包，无需编译
    :
}

lankebuild_package() {
    install -d "{STAGING_ROOT}{DATADIR}/unicode/cldr"
    cp -r common "{STAGING_ROOT}{DATADIR}/unicode/cldr/"

    install -Dm644 LICENSE \
        "{STAGING_ROOT}{DATADIR}/licenses/unicode-cldr/LICENSE"
}
