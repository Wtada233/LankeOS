#
# opus — 音频编解码器（Xiph.Org）
# 共享库构建

lankebuild_prepare() {
    set -e
}

lankebuild_build() {
    set -e

    ./configure \
        --prefix=/usr \
        --disable-static \
        --enable-custom-modes

    make -j$(nproc)
    make DESTDIR={STAGING_ROOT} install

    # 仅保留共享库
    rm -f "{STAGING_ROOT}/{LIBDIR}/libopus".a

    install -Dm644 COPYING "{STAGING_ROOT}/{DATADIR}/licenses/opus/COPYING"
}

lankebuild_package() {
    :
}
