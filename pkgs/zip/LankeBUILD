#!/bin/bash

lankebuild_prepare() {
    :
}

lankebuild_build() {
    make -f unix/Makefile generic CC="gcc -std=gnu89"
}

lankebuild_package() {
    make prefix={STAGING_ROOT}{PREFIX} MANDIR={STAGING_ROOT}{MANDIR}/man1 -f unix/Makefile install
}
