lankebuild_prepare() {
    cd "{SRC_DIR}"
    meson setup build --prefix=/usr --default-library=shared 
}
lankebuild_build() {
    cd "{SRC_DIR}"
    ninja -C build
}
lankebuild_package() {
    cd "{SRC_DIR}"
    DESTDIR="{STAGING_ROOT}" ninja -C build install
}
