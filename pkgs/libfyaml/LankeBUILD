lankebuild_prepare() {
    # Prepare environment
    ./configure --prefix=/usr --disable-static
}

lankebuild_build() {
    make -j$(nproc)
}


lankebuild_package() {
    make DESTDIR="{STAGING_ROOT}" install
}
