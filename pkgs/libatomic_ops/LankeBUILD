lankebuild_prepare() {
    :
}

lankebuild_build() {
    # release tarball 自带 configure（Arch 同款）
    ./configure --prefix={PREFIX} --disable-static --enable-shared
    make -j$(nproc)
    make DESTDIR={STAGING_ROOT} install
}

lankebuild_package() {
    :
}
