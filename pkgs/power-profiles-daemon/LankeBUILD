lankebuild_prepare() {
    # Prepare environment
    :
}

lankebuild_build() {
    meson setup build \
        --prefix={PREFIX} \
        --libexecdir=lib \
        -Dmanpage=disabled \
        -Dbashcomp=disabled \
        -Dgtk_doc=false \
        -Dtests=false
    meson compile -C build
    DESTDIR={STAGING_ROOT} meson install -C build
}

lankebuild_package() {
    # Installation handled by builder
    :
}
