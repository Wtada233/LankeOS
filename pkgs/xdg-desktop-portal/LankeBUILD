lankebuild_prepare() {
    # Prepare environment
    meson setup _build --prefix=/usr --buildtype=release -D tests=disabled
}

lankebuild_build() {
    meson compile -C _build -j$(nproc)
}


lankebuild_package() {
    DESTDIR="{STAGING_ROOT}" meson install -C _build
}
