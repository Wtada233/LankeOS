#!/bin/bash

lankebuild_prepare() {
    ./configure --prefix={PREFIX} --sysconfdir={SYSCONFDIR}
}

lankebuild_build() {
    make -j$(nproc)
    make DESTDIR={STAGING_ROOT} install
}

lankebuild_package() {
    :
}
