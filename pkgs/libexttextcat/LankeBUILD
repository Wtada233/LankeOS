lankebuild_prepare() {
    :
}

lankebuild_build() {
    # release tarball 自带 configure
    ./configure --prefix={PREFIX} --disable-static
    make -j$(nproc)
    make DESTDIR={STAGING_ROOT} install
}

lankebuild_package() {
    :
}
