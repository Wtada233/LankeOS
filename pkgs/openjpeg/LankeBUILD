lankebuild_prepare() {
    mkdir -p build
}

lankebuild_build() {
    echo "Configuring OpenJPEG-{ORIG_PKG_VER}"
    cd build
    cmake -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_INSTALL_PREFIX={PREFIX} \
          -DCMAKE_INSTALL_SYSCONFDIR={SYSCONFDIR} \
          -DBUILD_STATIC_LIBS=OFF ..
    echo "Building OpenJPEG-{ORIG_PKG_VER}"
    make -j$(nproc)
    echo "Build complete."
}

lankebuild_package() {
    echo "Packaging OpenJPEG-{ORIG_PKG_VER}"
    cd build
    make DESTDIR="{STAGING_ROOT}" install
    echo "Package staging complete."
}
